Build instructions
==================

The following commands will build the whole example suite
in a separate directory tree.

Linux & GNU Make
----------------

::
  mkdir _build
  cd _build
  qmake ../examples.pro
  make
