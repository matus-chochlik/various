CREATE TABLE config
WITH PRIMARY_KEY code NAME_STR;

ALTER TABLE config
ADD COLUMN value $(DESC_STR) NOT NULL;

GRANT SELECT ON config TO $(GUEST_ROLE);
GRANT SELECT ON config TO $(BROWSER_ROLE);
GRANT SELECT ON config TO $(EDITOR_ROLE);

IMPORT FUNCTION get_config_value;
